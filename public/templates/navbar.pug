nav.navbar.navbar-default
        .container-fluid
            .navbar-header
                button.navbar-toggle.collapsed(type='button' data-toggle='collapse' data-target='#navbar-collapse1')
                    span.sr-only Toggle
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href='/') VoteGen
                
            .collapse.navbar-collapse(id='navbar-collapse1')
                ul.nav.navbar-nav
                    li
                        a.nav-text(href='/polllist') Poll List
            
                if isHome || auth
                    ul.nav.navbar-nav.navbar-right
                        li
                            a.nav-text(href='/logout') Logout
                else
                    ul.nav.navbar-nav.navbar-right
                        li
                            a.nav-text(href='/signup') Sign Up
                        li
                            a.nav-text(href='/login') Login
